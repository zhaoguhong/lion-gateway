# 快速开始

## 环境要求

- JDK 21+
- Maven 3.6+

## 克隆项目

```bash
git clone <repository-url>
cd lion-gateway
```

## 编译项目

```bash
mvn clean package
```

## 运行项目

### 方式一：IDE 运行

直接运行主类：
```
com.zhaoguhong.lion.gateway.LionGatewayApplication
```

### 方式二：JAR 包运行

```bash
java -jar lion-gateway-bootstrap/target/lion-gateway-bootstrap-1.0-SNAPSHOT.jar
```

## 验证服务

服务启动后，监听 9999 端口。

### 测试 GET 请求

```bash
curl http://localhost:9999/test?param1=value1
```

### 测试 POST 请求

```bash
curl -X POST http://localhost:9999/test \
  -H "Content-Type: application/json" \
  -d '{"key":"value"}'
```

## 配置文件

`application.yml` 位置：
```
lion-gateway-bootstrap/src/main/resources/application.yml
```

配置示例见 [配置说明](./05-配置说明.md)。

## 项目结构说明

```
lion-gateway/
├── doc/                          # 文档（本目录）
├── lion-gateway-core/            # 核心功能模块
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/...        # 源代码
│   │   │   └── resources/      # 配置文件
│   │   └── test/               # 单元测试
│   └── pom.xml
├── lion-gateway-bootstrap/       # 启动模块
│   ├── src/main/
│   │   ├── java/...             # 主类
│   │   └── resources/         # application.yml
│   └── pom.xml
└── pom.xml
```

## 常见问题

### 端口被占用

修改 `NettyServer.java` 中的端口号：
```java
int port = 9999; // 修改为其他端口
```

### 虚拟线程未启用

检查 JDK 版本 >= 21，且 `application.yml` 中配置：
```yaml
spring:
  threads:
    virtual:
      enabled: true
```

## 下一步

- 了解 [架构设计](./02-架构设计.md)
- 学习 [插件开发](./04-插件开发.md)
- 查看 [配置说明](./05-配置说明.md)

